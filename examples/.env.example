# Microsoft/SharePoint Authentication Configuration
# Copy this file to .env and fill in your credentials

# Required: User email for Microsoft authentication
MS_AUTH_EMAIL=your.email@company.com

# Required: SharePoint/Microsoft URL to authenticate against
MS_SHAREPOINT_URL=https://yourcompany.sharepoint.com/

# Credential Configuration
# For simple password authentication (development/testing)
MS_AUTH_CREDENTIAL_TYPE=password
MS_AUTH_CREDENTIAL_PROVIDER=environment
MS_AUTH_ENV_VARIABLE_NAME=MS_USER_PASSWORD
MS_USER_PASSWORD=your-password-here

# Optional: Output directory for storage state files
# MS_AUTH_OUTPUT_DIR=~/.playwright-ms-auth

# Optional: Storage state expiration in hours (default: 24)
# MS_AUTH_STORAGE_STATE_EXPIRATION=24

# Optional: Microsoft Entra login endpoint (default: login.microsoftonline.com)
# MS_AUTH_LOGIN_ENDPOINT=login.microsoftonline.com

# ================================================================================
# DEBUGGING
# ================================================================================

# Enable detailed authentication flow logging (set to true for troubleshooting)
# When enabled, shows credential provider operations, browser launch params,
# storage paths, screenshots, and navigation events
# SYSTEM_DEBUG=true

# ================================================================================
# PRODUCTION CONFIGURATION (Azure KeyVault)
# For production, use Azure KeyVault instead of environment variables
# ================================================================================

# MS_AUTH_CREDENTIAL_PROVIDER=azure-keyvault
# MS_AUTH_KEYVAULT_ENDPOINT=https://your-vault.vault.azure.net
# MS_AUTH_KEYVAULT_SECRET_NAME=your-secret-name

# ================================================================================
# CERTIFICATE AUTHENTICATION
# For certificate-based authentication
# ================================================================================

# MS_AUTH_CREDENTIAL_TYPE=certificate
# MS_AUTH_CREDENTIAL_PROVIDER=local-file
# MS_AUTH_LOCAL_FILE_PATH=./path/to/cert.pfx
# MS_AUTH_CERTIFICATE_PASSWORD=optional-cert-password
